<div class="container">
	<h1 class="text-center">Welcome to VOCA!</h1>
	<br/>
	<br/>
	<p class="text-center">Go to the store's page to check the order queue and where your order is in line:-</p>
	<br/>
	<%= render 'store' %>
	<% if user_signed_in? %>
		<%= link_to 'Orders', orders_path %>
	<% end %>
</div>
<br/>